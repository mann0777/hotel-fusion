/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images:{
        domains:[
            "www.homestratosphere.com"
        ],
    }
};


module.exports = nextConfig